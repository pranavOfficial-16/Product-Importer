# Product Importer (FastAPI + Celery)

## Stack
- FastAPI (API + HTML templates)
- Celery (async workers)
- Redis (broker + progress tracking)
- PostgreSQL + SQLAlchemy
- Jinja2 templates

## Run (local)

1. Create virtualenv and install deps:

```bash
pip install -r requirements.txt
